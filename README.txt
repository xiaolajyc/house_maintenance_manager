# 房屋维护管理 PWA v22

Flat 结构：所有运行文件均在同一目录。

本版本更新：
- 使用“龙在修房子”作为 PWA App 图标
- 保留 v21 全部功能
- Service Worker 缓存版本升级为 v22
- localStorage + IndexedDB 数据结构保持不变

部署：
将本目录中的运行文件上传到 GitHub Pages 等 HTTPS 静态托管服务的仓库根目录。
